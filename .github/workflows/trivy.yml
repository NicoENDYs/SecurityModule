name: Trivy Security Scan

on:
  pull_request:
    branches: ["main", "master", "develop"]
  push:
    branches: ["main", "master"]
  schedule:
    # Run every Monday at 06:00 UTC
    - cron: "0 6 * * 1"
  workflow_call:
    inputs:
      image-ref:
        description: "Docker image to scan (leave empty to scan filesystem only)"
        required: false
        type: string
        default: ""
      severity:
        description: "Comma-separated severity levels to report"
        required: false
        type: string
        default: "HIGH,CRITICAL"
      fail-on-findings:
        description: "Exit with error code if vulnerabilities are found"
        required: false
        type: boolean
        default: false

permissions:
  contents: read
  security-events: write  # Required to upload SARIF results
  actions: read

jobs:
  trivy-fs:
    name: Filesystem Scan
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run Trivy — filesystem scan
        uses: aquasecurity/trivy-action@0.31.0
        with:
          scan-type: "fs"
          scan-ref: "."
          trivy-config: ".trivy.yaml"
          severity: ${{ inputs.severity || 'HIGH,CRITICAL' }}
          exit-code: ${{ inputs.fail-on-findings && '1' || '0' }}
          ignore-unfixed: true
          format: "sarif"
          output: "trivy-fs-results.sarif"

      - name: Upload Trivy FS scan results to GitHub Security tab
        uses: github/codeql-action/upload-sarif@v3
        if: always()
        with:
          sarif_file: "trivy-fs-results.sarif"
          category: "trivy-filesystem"

      - name: Run Trivy — table output (PR comment preview)
        uses: aquasecurity/trivy-action@0.31.0
        with:
          scan-type: "fs"
          scan-ref: "."
          severity: ${{ inputs.severity || 'HIGH,CRITICAL' }}
          exit-code: "0"
          ignore-unfixed: true
          format: "table"

  trivy-image:
    name: Docker Image Scan
    runs-on: ubuntu-latest
    if: ${{ inputs.image-ref != '' }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run Trivy — image scan
        uses: aquasecurity/trivy-action@0.31.0
        with:
          scan-type: "image"
          image-ref: ${{ inputs.image-ref }}
          severity: ${{ inputs.severity || 'HIGH,CRITICAL' }}
          exit-code: ${{ inputs.fail-on-findings && '1' || '0' }}
          ignore-unfixed: true
          format: "sarif"
          output: "trivy-image-results.sarif"

      - name: Upload Trivy image scan results to GitHub Security tab
        uses: github/codeql-action/upload-sarif@v3
        if: always()
        with:
          sarif_file: "trivy-image-results.sarif"
          category: "trivy-image"

  trivy-config:
    name: IaC / Misconfiguration Scan
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run Trivy — config scan (IaC misconfigurations)
        uses: aquasecurity/trivy-action@0.31.0
        with:
          scan-type: "config"
          scan-ref: "."
          severity: ${{ inputs.severity || 'HIGH,CRITICAL' }}
          exit-code: "0"
          format: "sarif"
          output: "trivy-config-results.sarif"

      - name: Upload Trivy config scan results
        uses: github/codeql-action/upload-sarif@v3
        if: always()
        with:
          sarif_file: "trivy-config-results.sarif"
          category: "trivy-config"
